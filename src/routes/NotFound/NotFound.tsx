import "./NotFound.scss";

// interface NotFoundProps {
//
// }

/**
 * A default route to display when no other route has matched with the router.
 */
export function NotFound() {
    return (
        <div className="route-not-found">NotFound</div>
    );
}
